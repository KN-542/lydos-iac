# AWS設定
AWS_ACCOUNT_ID=000 ## 適切な値を.envには入れてください
AWS_REGION=ap-northeast-1

# ドメイン設定
DOMAIN_NAME=lydos.click
# サブドメイン（例: www, app など。ルートドメインのみの場合は空欄）
SUBDOMAIN=www

# GitHub設定
GITHUB_OWNER=KN-542
GITHUB_REPO=lydos-view
GITHUB_BRANCH=main
# AWS Secrets Managerに保存されているGitHub Tokenのシークレット名
GITHUB_TOKEN_SECRET_NAME=github-token

# Amplify設定
AMPLIFY_APP_NAME=lydos-view

# Database設定
# RDS: db.t3.micro (無料枠12ヶ月、暗号化サポート), db.t4g.micro (安い), db.t3.small など
RDS_INSTANCE_TYPE=db.t3.micro
# Redis: cache.t3.micro (無料枠12ヶ月), cache.t4g.micro (安い) など
REDIS_NODE_TYPE=cache.t3.micro

# 環境
ENVIRONMENT=production

# DatabaseStack デプロイ後に Secrets Manager から取得して設定
DB_PASSWORD=
